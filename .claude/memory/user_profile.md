---
name: User profile
description: Power user working on Whisper transcription and subtitle notebooks in Google Colab
type: user
---

Technical user focused on Google Colab Python notebooks for audio transcription (OpenAI Whisper) and ASS subtitle processing (ffmpeg libass).

Works iteratively: requests detailed forensic analysis, structured feature proposals, and complete change lists before any implementation. Strong preference for approval gates — never modify files without the list being reviewed first.

Prefers terse, direct responses — no filler, no preamble, no trailing summaries. Uses GitHub for all version control with manual token auth. Pushes are non-negotiable after every change.

Aware this sandbox is ephemeral — keeps all config in the repo at `/home/vercel-sandbox/Whisper_Notebooks/` so it survives session resets.
